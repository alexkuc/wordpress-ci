version: v1.0
name: WordPress CI Pipeline

auto_cancel:
  running:
    when: "branch != 'master'"

fail_fast:
  stop:
    when: "branch != 'master'"

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

execution_time_limit:
  minutes: 10

blocks:

  - name: Prepare cache
    dependencies: []
    task:
      prologue:
        commands:
          - checkout
      jobs:
        - name: Composer cache (--dev)
          commands:
            - .semaphore/composer/cache-restore.sh
            - scripts/setup.sh
            - .semaphore/composer/cache-store.sh
        - name: NodeJS cache
          commands:
            # TODO: setup caching of NodeJS deps
            - echo '...'

  - name: Linters
    dependencies: ['Prepare cache']
    task:
      prologue:
        commands:
          - checkout
      jobs:
        - name: ShellCheck
          commands:
            - scripts/lint/lint.sh 'shellcheck'
        - name: phplint
          commands:
            - .semaphore/composer/cache-restore.sh
            - scripts/lint/lint.sh 'phplint'
        - name: phpcs
          commands:
            - .semaphore/composer/cache-restore.sh
            - scripts/lint/lint.sh 'phpcs'

  - name: PHP Tests (docker-compose.yml)
    dependencies: ['Prepare cache']
    task:
      prologue:
        commands:
          - checkout
          - .semaphore/composer/cache-restore.sh
          - sudo chmod -R 777 .
          - scripts/up.sh
      epilogue:
        on_fail:
          commands:
            - artifact push job test/tests/_output/
      jobs:
        - name: acceptance
          commands:
            - scripts/test.sh acceptance
        - name: functional
          commands:
            - scripts/test.sh functional
        - name: wpunit
          commands:
            - scripts/test.sh wpunit
        - name: unit
          commands:
            - scripts/test.sh unit
    task:
      name: container + service
      jobs:
        - name: acceptance
          commands:
            - echo '...'

  - name: Assets
    dependencies: ['Prepare cache']
    task:
      prologue:
        commands:
          - checkout
      jobs:
        - name: Minifier (JS)
          commands:
            # TODO: complete JS minifier
            - echo '...'
        - name: Minifier (CSS)
          commands:
            # TODO: complete CSS minifier
            - echo '...'

  - name: Deploy
    dependencies: ['Linters', 'PHP Tests (docker-compose.yml)', 'Assets']
    # TODO: uncomment
    # skip:
    #   when: "branch != 'master'"
    task:
      prologue:
        commands:
          - checkout
          - export CI_COMPOSER_NO_DEV='1'
          - .semaphore/composer/cache-restore.sh
          - sudo chmod -R 777 .
      jobs:
        - name: Theme
          commands:
            # TODO: complete deploy stage
            - echo '...'
